import 'package:flutter/material.dart';

class UserDoor extends StatelessWidget {
  const UserDoor({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        "Tür - Seite",
        style: TextStyle(fontSize: 50),
      )
    );
  }
}